mixin alert-success(time)
  - if (messages.success)
    .message.info
      div.alert.alert-success(show-alert, data-time=time)
        | #{messages.success}
        button.alert-close(close-alert, type="button")
          | &times;

mixin alert-error(time)
  - if (messages.error)
    .message.info
      div.alert.alert-danger(show-alert, data-time=time)
        | #{messages.error}
        button.alert-close(close-alert, type="button")
          | &times;